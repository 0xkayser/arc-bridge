{
  "name": "arc-bridge",
  "version": "1.0.0",
  "description": "Self-custody Base USDC deposit and Circle Gateway self-mint flow to Arc mainnet.",
  "main": "bridge.js",
  "scripts": {
    "check": "node --check bridge.js && node --check web/app.js && python3 -m py_compile arc_gateway_bridge.py",
    "web": "python3 -m http.server ${PORT:-8080} --directory web",
    "test": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "viem": "^2.55.10"
  }
}
