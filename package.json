{
  "name": "arc-bridge",
  "version": "1.0.0",
  "description": "A self-custody Base USDC deposit and Circle Gateway forwarded-transfer flow using the official Gateway contracts and REST API.",
  "main": "bridge.js",
  "scripts": {
    "check": "node --check bridge.js && node --check web/app.js",
    "web": "python3 -m http.server ${PORT:-8080} --directory web",
    "test": "npm run check"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "viem": "^2.55.10"
  }
}
